
const NoteEditor = () => {

  return(
    <div className="">
        TODO: criar o componente de escrita de notas, com funcionalidades:
        -salvar, editar e deletar notas.
        -forma de organizar as notas (tags, categorias, etc).
        -formatacao de texto (negrito, italico, listas, etc).
    </div>
  )
};

export default NoteEditor;